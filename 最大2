a = input().split()
c = input().split()

jason = []
for i in range(len(a))  :
    for  j in range(1,:a[i]) :
        if int(a[i]) > int(c[i]) :
            int(a[i]) % int(c[i]) == 0
            jason.insert(i,int(c[i]))
            if 
                x = a
        

        elif int(c[i]) > int(a[i]) :
            int(c[i]) % int(a[i]) == 0
            jason.insert(i,int(a[i]))
    
        else:
            int(a[i])
            int(c[i]) 
    
    
print(jason)
